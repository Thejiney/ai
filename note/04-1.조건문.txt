조건문 종류
if, switch~case, 삼항연산자
if : else if와 else를 통해 여러 조건문을 활용 가능.

if : 조건을 통하여 구문을 수행할때 사용한다.

switch~case : switch(조건) 정수, 문자, 문자열을 통하여 case(조건) 구문을 실행 가능
	     break;를 통하여 꼭 끊어주어야 후행 구문이 실행되지 않음

삼항연산자 : if와 비슷한 논리구조를 가지며 (조건) ? "참" : (조건) ? "참": "거짓" 을 통하여 여러 조건 활용 가능
	    조건을 통해 값을 할당할 때 사용.