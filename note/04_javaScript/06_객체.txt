서로 연과된 변수와 함수를 그룹핑하고 이름을 붙인 것.

배열 내부에 있는 값을 요소 : element
객체 내부에 있는 값을 속성 : property
자바 스크립트에서 요소와 속성을 구분하여 사용하지 않음.

객체의 속성중 함수형 자료형인 속성 : 메소드

객체의 속성의 추가와 제거가 코드 내부에서 이루어질 수 있다.
(자바와 같이 클래스가 필요하지 않음)